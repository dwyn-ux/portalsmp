RewriteEngine On

# Serve existing files/dirs directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route everything to index.php (PHP router handles all routes)
RewriteRule ^ index.php [L,QSA]
